import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vishalmysore.easyqz',
  appName: 'EasyQZ',
  webDir: 'dist/easy-q/browser',

};

export default config;
